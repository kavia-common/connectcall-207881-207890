# Backend configuration (do not commit real secrets)

# JWT
JWT_SECRET=change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# CORS
# Set to the exact preview frontend origin, e.g. https://<host>:3000
FRONTEND_ORIGIN=
# Or use comma-separated list:
FRONTEND_ORIGINS=

# PostgreSQL is provided by platform/container environment:
# POSTGRES_URL, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, POSTGRES_PORT
